const startGame = () => {
    const startGameBtn = document.createElement("button")
    startGameBtn.classList.add("start-game");
    startGameBtn.innerHTML("Start Game");

    startGameBtn.addEventListener("click", () => {
        //hide start-menu and generate the board;
    }); 
}